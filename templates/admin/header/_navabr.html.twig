<nav class="navbar navbar-expand-lg shadow sticky z-50 top-0 py-2 navbar-dark bg-primary w-full">
    <div class="container-fluid">
        <a class="flex justify-center navbar-brand items-baseline" href="{{ path('home') }}">
            <i class="fas fa-user-shield text-2xl mr-2"></i>
            {{ 'app.admin.components._nav.brand'|trans }}

        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            {% include 'admin/header/components/_nav_search.html.twig' %}

            <ul class="navbar-nav ml-auto mr-9">

                {% include 'admin/header/components/_nav_links.html.twig' %}

            </ul>

            <ul class="navbar-nav ">

                {% include 'admin/header/components/_nav_locale_dd.html.twig' %}

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                    {% include 'admin/header/components/_auth_links.html.twig' %}

                {% else %}

                    {% include 'admin/header/components/_unauth_links.html.twig' %}

                {% endif %}
            </ul>

        </div>
    </div>
</nav>